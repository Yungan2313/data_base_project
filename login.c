#include <stdio.h>
#include <string.h>
#include <stdlib.h>

void account(){
    char* account_name;
    account_name = (char*)malloc(sizeof(char) * 21);
    fgets(account_name, 21, stdin);
}
void password(){

}

